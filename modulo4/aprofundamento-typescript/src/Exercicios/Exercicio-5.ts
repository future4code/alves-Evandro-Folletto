// a)
// O que mudou:
  // Opções que estavam nos slides, mas não no arquivo gerado:
    // sourceMap, outDir, rootDir, removeComments, noImplicitAny
  // Opções que estavam no arquivo gerado, mas não nos slides:
    // esModuleInterop, forceConsistentCasingInFileNames, strict, skipLibCheck